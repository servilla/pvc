# Package Version Checker (PVC)

Check the current installed version of a Python package.

PVC interrogates a Python project's environment.yml file (if Conda-based) or pixi.lock file (if Pixi-based) for the specified package and, if present, attempts to parse out and reports its version.
